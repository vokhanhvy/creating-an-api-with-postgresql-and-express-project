{
  "dev": {
    "driver": "pg",
    "user": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}",
    "host": "${POSTGRES_HOST}",
    "database": "${POSTGRES_DB}",
    "port": "${POSTGRES_PORT}"
  }
}
